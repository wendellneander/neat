{
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/main.js",
    "nn": "tsc && node dist/train-neural-network.js"
  },
  "devDependencies": {
    "@types/node": "^20.9.1",
    "typescript": "^5.2.2"
  }
}
